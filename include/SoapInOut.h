#ifndef SOAP_IN_OUT_H
#define SOAP_IN_OUT_H

#include <QString>
#include "SoapProfiles.h"

struct IngredientInput {
    qint32 profileId = -1;
    qreal amount = 0;
};

struct RecipeInput {

};

#endif // SOAPINOUT_H
